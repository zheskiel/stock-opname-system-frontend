const initialState = {
  isAuth: false,
  success: false,
  message: "",
  data: [],
  loading: false,
};

const Auth = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Auth;
